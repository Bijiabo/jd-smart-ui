import UI from "../core/UI";
var $ = require('jquery');

/**
 * new Selector 
 * 
 * @class Selector
 * @extends {UI}
 */
class Selector extends UI {
    constructor(options) {
        super(options);
    }
}